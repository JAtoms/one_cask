enum FontType { lato, ebGaramond }

enum TabType { details, tasting, history }

TabType details = TabType.details,
    tasting = TabType.tasting,
    history = TabType.history;

FontType lato = FontType.lato, ebGaramond = FontType.lato;
